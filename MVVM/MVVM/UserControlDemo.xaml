<UserControl x:Class="MVVM.UserControlDemo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="31*"/>
            <RowDefinition Height="44*"/>
        </Grid.RowDefinitions>
        <Button x:Name="Button_Team" Content="Team" HorizontalAlignment="Left" Margin="109,50,0,0" VerticalAlignment="Top" Width="75" Click="Button_Team_Click"/>
        <ListView x:Name="ListView1" Visibility="Hidden" HorizontalAlignment="Left" Height="100" Margin="102,75,0,0" VerticalAlignment="Top" Width="100" Grid.RowSpan="2">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
